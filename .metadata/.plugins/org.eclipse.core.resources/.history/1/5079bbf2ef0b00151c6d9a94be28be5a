package automationMobile;

import java.util.Comparator;

public class compareList {
	String nome;
	
	
	public interface Comparator<T> {
	    int compare(T o1, T o2);
	
	public abstract class ListHostel implements Comparator<sortByName> {
		
		public int compareTo(sortByName allItems){
			return this.nome.compareTo(allItems.getClass());

		}
	}
}